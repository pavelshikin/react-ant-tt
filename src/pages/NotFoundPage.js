import React from 'react';

const NotFoundPage = () => (
  <div className={'container'}>
    <h1 className={'t-center my-2'}>404</h1>
    <h2 className={'t-center'}>Page not found</h2>
  </div>
);

export default NotFoundPage;
